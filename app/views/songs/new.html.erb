<%=form_for @song, url: artist_songs_path, html: { method: :post } do |f| %>

<div>
  <p><%= f.label :title %></p>
<%= f.text_field :title %>
</div>

<div>
  <%= f.submit "Save" %>
  <% end %>
</div>
